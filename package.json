{
  "name": "base-goerli-platform",
  "license": "UNLICENSED",
  "scripts": {
    "test": "graph test",
    "prepare:base": "mustache config/base.json subgraph.template.yaml > subgraph.yaml",
    "prepare:base-goerli": "mustache config/base-goerli.json subgraph.template.yaml > subgraph.yaml",
    "prepare:optimism": "mustache config/optimism.json subgraph.template.yml > subgraph.yaml",
    "prepare:optimism-goerli": "mustache config/optimism-goerli.json subgraph.template.yml > subgraph.yaml"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.53.0",
    "@graphprotocol/graph-ts": "^0.31.0"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0",
    "mustache": "^4.2.0"
  }
}
